language: node_js

node_js:
  - "lts/*"

cache:
  directories:
    - "node_modules"
    - "test/simulation/go" # lnd, btcd, and dependencies

before_install:
  - eval "$(GIMME_GO_VERSION=1.12 gimme)"

script:
  - npm run lintNoFix
  - npm run test
  - npm run test:sim
